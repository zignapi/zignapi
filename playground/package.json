{
  "name": "playground",
  "version": "0.0.0",
  "private": true,
  "description": "Example zigbind addon exercising the full build/test pipeline",
  "scripts": {
    "build": "zigbind build",
    "test": "node --test"
  },
  "dependencies": {
    "zignapi": "workspace:*"
  }
}
