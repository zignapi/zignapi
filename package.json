{
  "name": "zigbind-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Write native Node.js addons in Zig (napi-rs, but for Zig)",
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@11.8.0",
  "scripts": {
    "build": "tsc -p packages/zigbind",
    "prepare": "tsc -p packages/zigbind"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2"
  }
}
